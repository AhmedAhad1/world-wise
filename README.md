# Repeating the boring fundamentals

## takeaways:
 1. react router dom
 2. useParams
 3. const [searchParams,setSearchParams]= useSearchParams()
   searchParams.get(`${urlVariableName}`)
 4. nested routing && index routing
 5..Outlet component
